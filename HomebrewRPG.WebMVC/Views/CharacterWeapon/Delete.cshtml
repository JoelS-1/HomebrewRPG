@model HomebrewRPG.Models.CharacterWeaponModels.CharacterWeaponDetail

@{
    ViewBag.Title = "Delete";
}

<div class="row padding">

    <div class="col-sm-12 col-md-6 col-md-offset-2 text-center">
        <h2>Delete</h2>

        <h3>Are you sure you want to remove this item from your inventory?</h3>
        <div>
            <hr />
            <dl class="dl-horizontal">

                <dt>
                    @Html.DisplayNameFor(model => model.WeaponName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.WeaponName)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.Description)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Description)
                </dd>
            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="row padding">
                    <input type="submit" value="Remove Weapon Forever" class="btn bg-danger" />
                </div>
            }
            <div class="row padding">
                <a href='@Url.Action("Index", "Weapon")' class="btn btn-default">Back to Weapons</a>
            </div>
        </div>
    </div>
</div>