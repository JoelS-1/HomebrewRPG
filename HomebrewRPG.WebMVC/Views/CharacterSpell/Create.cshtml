@model HomebrewRPG.Models.CharacterSpell.CharacterSpellCreate

@{
    ViewBag.Title = "Create";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal padding">
        <h4>Add Item</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.SpellId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.SpellId, ViewBag.SpellId as SelectList, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.SpellId, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.CharacterId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.CharacterId, ViewBag.CharacterId as SelectList, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.CharacterId, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Add" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div class="row padding">
    <a href='@Url.Action("Index", "Character")' class="btn btn-default btn-sm">Back to Characters</a>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
